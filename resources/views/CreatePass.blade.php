<div class="container w-50 mt-4">
    <div class="card">
        <div class="card-header">Create Pass</div>
        <div class="card-body">
            {{-- <div class="row">
                <div class="col-6">
                    <label for="exampleInputEmail1" class="form-label">Organizer</label>
                    <input type="text" class="form-control" placeholder="Organizer" aria-label="First name"
                        id="organizer" name="organizer" value="{{ $organizer }}">
                </div>
                <div class="col-6">
                    <label for="exampleInputEmail1" class="form-label">Pass Type</label>
                    <select class="form-select" aria-label="Default select example">
                        <option selected>Open this select menu</option>
                        @foreach ($passType as $key => $value)
                            <option value="{{ $key }}">{{ $value }}</option>
                        @endforeach
                    </select>
                </div>
                <div class="col-6 mt-4">
                    <label for="timezone" class="form-label">Time Zone</label>
                    <select class="form-select" aria-label="Default select example">
                        <option selected>Select TimeZone</option>
                        @foreach ($timezone as $key => $value)
                            <option value="{{ $key }}">{{ $value }}</option>
                        @endforeach
                    </select>
                </div>
            </div> --}}
        </div>
    </div>
</div>
